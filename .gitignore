# ── Secrets ──
.env
.env.*
!env.example
!.env.example
**/.env
*.key
*.pem
*.crt

# ── Data ──
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# ── Build ──
build/
tmp/
dist/

# ── Python ──
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
*.egg

# ── Node ──
node_modules/
web/dist/

# ── IDE ──
.idea/
.vscode/
*.swp
*.swo

# ── OS ──
.DS_Store
Thumbs.db

# ── Generated ──
generated/
agent/.skill_state.json

# ── Binaries / archives (added 2026-05-13) ──
*.exe
*.zip
bin/
agent_outputs/
doc/history/agent_outputs/
*.log

# ── One-shot scripts archive (pre-existing, do not recover) ──
scripts/archive/

# ── Historical leaked-secret material (do NOT upload) ──
scripts/deploy/
scripts/monitor_logs.py
doc/history/
修复报告.md
架构评审报告.md
部署验证报告.md

# ── Local-only ad-hoc test files ──
cmd/_test_*/

# ── Claude Code per-user settings (URLs / paths leak otherwise) ──
.claude/settings.local.json
